// TODO: support force validation on internal classes e.g. validateCommand(command) before adding to DefaultCommandRegistry
// TODO: validate default command if specified before passing to default runner
// TODO: populate configured values from ENV VARS and config file
// TODO: validate cliconfig.defaultValues values for command before providing them in context and setting them in config:
//  i.e. make sure values match declarations and make sure values are for a known command. assert max nesting and array sizes
// TODO: printer service —theme solarized-dark/light
// TODO: password type
